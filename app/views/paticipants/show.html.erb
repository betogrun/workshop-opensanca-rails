<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @paticipant.name %>
</p>

<p>
  <strong>Event:</strong>
  <%= @paticipant.event_id %>
</p>

<%= link_to 'Edit', edit_paticipant_path(@paticipant) %> |
<%= link_to 'Back', paticipants_path %>
